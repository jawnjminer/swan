# Copy to .env.local and fill in your Supabase project values to enable
# cross-device sync between the Instructor Console and the bedside monitor.
# When these are absent, the app runs as a single-device simulator (no sync).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
